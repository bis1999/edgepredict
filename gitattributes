# Normalize line endings across platforms
* text=auto eol=lf

# Treat source/config/docs as text explicitly
*.py   text eol=lf
*.sh   text eol=lf
*.md   text eol=lf
*.txt  text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# Jupyter notebooks: strip outputs on commit (requires nbstripout)
*.ipynb filter=nbstripout

# OPTIONAL: If you want Git LFS for large binaries, uncomment these lines:
# *.pkl    filter=lfs diff=lfs merge=lfs -text
# *.pt     filter=lfs diff=lfs merge=lfs -text
# *.pth    filter=lfs diff=lfs merge=lfs -text
# *.joblib filter=lfs diff=lfs merge=lfs -text
